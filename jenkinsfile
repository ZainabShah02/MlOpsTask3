pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        
        stage('Install Requirements') {
            steps {
                powershell 'pip install -r requirements.txt'
            }
        }
        
        stage('Run Tests') {
            steps {
                powershell 'python test_file.py'
            }
        }
    }
}
